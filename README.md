# ALPHAFOLIO

### Prerequisites (windows)

1. Install pipx : `pip install pipx`

2. Install uv from pipx: `pipx install uv`

If linux:

```
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### How to run the repo

```
uv run src/alphafolio/main.py
```
